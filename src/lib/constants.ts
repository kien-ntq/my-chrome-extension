/** Keys available for selecting tabs on the current page. j/k are reserved for item navigation. */
export const shortcutKeys = 'asdfghlqwertyuiopzxcvbnm';

/** Fixed page size so the popup list does not need overflow scrolling. */
export const PAGE_SIZE = 10;